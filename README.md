# CSV Reader
Read and generate stat summary and graphs for CSV files.
## Installation
```pip install -r requirements.txt```
## Usage
Ensure csv file is in the same directory as csv_reader.py. 
```python csv_reader.py```